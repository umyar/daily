# Host credential for starting and ending sessions. Never commit the real value.
# Local: this file's real counterpart, .env, is gitignored.
# Production: set HOST_PASSWORD in the Vercel dashboard (Settings > Environment Variables).
# No VITE_ prefix — that would inline it into the public client bundle.
HOST_PASSWORD=
